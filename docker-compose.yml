services:
  postgres:
    image: postgres:15
    container_name: bikeshare-postgres
    environment:
      POSTGRES_DB: bikeshare
      POSTGRES_USER: bikeshare
      POSTGRES_PASSWORD: bikeshare
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
